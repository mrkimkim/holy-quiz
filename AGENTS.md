# AGENTS.md — 말씀퀴즈 프로젝트 에이전트 가이드

> 다음 세션(에이전트)이 맥락을 이어받기 위한 핸드오프 문서. 작업 히스토리와 앞으로의 방향을 기술한다.

## 1. 이 저장소는

**말씀퀴즈(가칭)** — 교회 대항 성경 퀴즈 앱. 중장년(45세+) 한국 개신교인 대상 모바일 퀴즈 게임.
개인의 **명예(등급)** 와 **"우리 교회"의 리더보드 순위**를 위해 매일 겨루는 것이 핵심이며, 구역 카톡방을 타는 **교회 단위 바이럴이 성장 엔진**이다.

## 2. 현재 상태 (2026-07-12 기준)

저장소는 **의도적으로 단순화된 상태**다. 문서 3개만 존재한다.

| 파일 | 성격 |
|---|---|
| `docs/PRD.md` | **High-Level PRD (현재 기준 문서)** — 서비스 컨셉·사용자 경험에 집중. 세부 메커니즘은 의도적으로 제외. |
| `docs/functional-spec.md` | **기능 명세서** — 전체 기능 + 상세 동작 + 모든 사용자 시나리오(정상/경계/오류/악용) + E2E 여정. 단 **화면 구성(UI)은 제외**. 확정 수치는 `[튜닝]` 표기. |
| `docs/PRD-v1.0.md` | 최초 업로드된 **상세 원본 PRD** (참고용). |

- **기본 브랜치: `main`** (원격 `mrkimkim/holy-quiz`). `claude/service-planning-review-2ow992` 브랜치도 동일 내용이라 정리 가능.
- **GitHub 이슈: 전부 닫힘(open 0).**

## 3. 문서 원칙 (사용자가 세운 경계 — 반드시 지킬 것)

1. **High-Level PRD(`PRD.md`)에 세부 메커니즘을 넣지 않는다.** 포인트 값·등급 임계치·광고 타이밍/트리거·콤보·어뷰징 규칙·가격·KPI 수치 등 제외. 컨셉과 UX만.
2. **기능 명세서(`functional-spec.md`)에 화면 구성을 넣지 않는다.** 레이아웃/버튼/탭/배치 등 UI 표현 금지. "무엇을 하고, 시스템이 어떻게 반응하며, 사용자가 무엇을 경험하는가"만 기술.
3. **확정 수치·산식·내부 규칙은 `[튜닝]`/`[상세 설계]`로 유보한다.** 밸런싱·상세 설계는 사용자가 별도로 지시할 때 작성.
4. **단순함을 우선한다.** 사용자는 조기 과잉설계를 경계한다. **요청 범위를 넘어 문서·구조를 늘리지 말 것.**

## 4. 작업 히스토리

이 프로젝트는 한 번 크게 확장됐다가 사용자 지시로 다시 단순화되었다. **삭제된 산출물은 전부 git 히스토리에 보존**되어 있어 복구 가능하다.

| 커밋 | 내용 |
|---|---|
| `1ec98db` | 최초 PRD 추가 |
| `0341541` | **대규모 기획 문서 세트** — 서비스 이해, 갭 분석(28건), 파트별 실무문서 13종(브랜딩/디자인/UX/클라이언트/서버/수익화/마케팅/데이터/콘텐츠/법무/QA/운영/조직), 3렌즈 상호검증 리포트, 마일스톤 로드맵 |
| `584ec81`, `21f19e6` | **M0 의사결정 로그** 및 결정 확정 |
| `1c6f3ac` | **ADR-001**: GCP+Firebase 서버리스 채택 |
| `b9d5657` | **모노레포 스캐폴드** — Flutter app + TS Cloud Functions + Firebase 설정 (functions 빌드/lint 검증 완료) |
| `4f2cd02` | ⬅ **단순화 지점** — 초기 PRD만 남기고 전부 제거 (사용자 지시) |
| `fbdd8c8` | **High-Level PRD**(`PRD.md`) 작성 |
| `7aac4e4` | **기능 명세서**(`functional-spec.md`) 작성 |

또한 GitHub에 트래커(인덱스 #1 + Epic 14개 + M0 티켓 15개, sub-issue 연결)를 만들었다가 **전부 닫음**. 필요 시 reopen 가능.

### 삭제된 자료 복구
`git show <commit>:<path>` 로 꺼낼 수 있다. 참고 가치가 큰 것:
- 파트별 실무문서 13종 · 로드맵 · 상호검증 → `0341541:docs/parts/*.md`, `0341541:docs/40-roadmap.md`, `0341541:docs/30-cross-verification.md`
- M0 결정 로그 → `584ec81:docs/M0-decision-log.md`
- ADR-001(Firebase) → `1c6f3ac:docs/adr/ADR-001-gcp-firebase.md`
- 스캐폴드 → `b9d5657` (app/, functions/, firebase.json, firestore.rules 등)

## 5. 앞서 내려진 결정 (참고 — 이후 단순화로 문서는 지웠으나 유효한 방향)

컨셉 수준에서 사용자가 확정한 방향. 상세 근거는 위 git 히스토리에 있음.

- **앱명:** 말씀퀴즈
- **표기:** 개역개정 표기 통일. v1 본문은 **근거구절 주소만 표시**(전문 미표시 = 저작권 라이선스 회피 폴백)
- **주간 리셋:** 월요일 00시 (파일럿에서 A/B 여지)
- **교회 점수 합산 상위 N:** 하드코딩 금지, **파라미터화**(파일럿 10/20/30 실험)
- **v1 범위:** 개신교 한정
- **브랜딩:** 로고 = 펼친 성경 위 등불빛 · 최고 등급(면류관) = 월계관형 · 컬러 = 딥블루 `#1D4E89` + 웜골드 + 크림 · 초대 명칭 = "초대왕"
- **v1 초대 귀속:** 6자리 코드 수동귀속 (딥링크 자동귀속은 후순위)
- **보상형 광고:** 배율분은 **교회 대항 합산에서 제외**(명예 공정성)
- **기술 스택:** **GCP + Firebase 서버리스** (Cloud Functions TypeScript · Firestore · FCM · Remote Config · Analytics+BigQuery · App Check) + **Flutter + Riverpod 2.x + go_router**
  - **서버 권위 원칙(중요):** 점수·포인트 원장·리더보드·문제 정답은 클라이언트가 직접 쓰지 못하고 **Cloud Functions로만 변경**한다(Firestore 보안 규칙으로 강제). 교회 대항전 조작 방어의 핵심.

**보류(사용자가 뒤로 미룸 — 출시 전 재개 필요, ⚠ = 법적 필수):**
- 사업 목표/KPI 재정의, 사업자 등록, 자금 조달, 인력 채용
- ⚠ 개인정보(민감정보=종교) 동의·처리방침, 종교 타깃 광고 정책, 상표·인허가

## 6. 앞으로의 방향

사용자는 **기능별 상세 설계·문서를 하나씩 지시**할 계획이다. 예상 흐름:

1. (완료) High-Level PRD → 기능 명세서.
2. (다음) 사용자가 개별 기능/영역의 **상세 설계**를 지시 → `[튜닝]`으로 유보했던 수치·규칙·산식을 확정하는 문서 작성.
3. 이후 UX/화면 설계, 그다음 실제 구현(스캐폴드는 히스토리에서 복구).

**다음 에이전트가 할 일:**
- 사용자가 요청한 **정확한 범위만** 수행. §3 문서 원칙 준수.
- 상세 설계 문서는 `functional-spec.md`의 `[튜닝]` 지점과 §5(유보 목록)를 출발점으로 삼는다.
- 새 문서는 `docs/`에 두고 커밋 후 `main`에 푸시.

## 7. 운영 규칙 (환경 특이사항)

- 커밋·푸시는 **`main`** 기준. 원격 = GitHub `mrkimkim/holy-quiz`.
- **GitHub 저장소 설정 변경(기본 브랜치 등)은 이 세션 도구로 불가** — 직접 GitHub API 접근이 정책상 차단됨(403). GitHub 작업은 MCP 도구로만 가능하며 저장소 설정 변경 기능은 없음. → 그런 변경은 사용자가 웹에서 처리한다.
- 문서는 **한국어**로 작성.
- 작업 후 원격 반영 상태를 확인해 보고한다(사용자가 중시).

## 8. 사용자에 대해

- 기술 배경 있음(SWE, 모바일·광고 도메인 이해도 높음).
- 점진적·통제적으로 진행 — **한 번에 하나씩**, 조기 과잉설계를 경계.
- 결과가 원격(main)에 정확히 반영됐는지 확인하는 것을 중시.
