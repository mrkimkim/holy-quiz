name: malssum_quiz
description: "교회 대항 성경 퀴즈 앱 (말씀퀴즈)"
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  # 상태관리·라우팅 (결정: 13-client.md)
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1
  go_router: ^14.6.2
  # Firebase (ADR-001)
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.5.1
  firebase_messaging: ^15.1.6
  firebase_remote_config: ^5.1.5
  firebase_analytics: ^11.3.6
  firebase_crashlytics: ^4.1.6
  intl: ^0.19.0

dev_dependencies:
  flutter_lints: ^5.0.0
  build_runner: ^2.4.13
  riverpod_generator: ^2.6.3
  custom_lint: ^0.7.0
  riverpod_lint: ^2.6.3

flutter:
  uses-material-design: true
